## TODO:

1. [DONE] Change permission system to be blocking and user facing only, no more manager agent approval. No permission que, tool execution waits for user to aproove or cancel (with obligatory reason why he did). If aproved the tool call continues as normal, if not an user mesage will be inserted with the reason why it was canceled. Will affect any level of agent, the user can see the window of the working agent so it has a lot more context than any manager instance.
2. Change agent soul.md files to use AgentClass_soul.md format, all in one dir
3. When calling an agent we must select the agent class and a name for agent instance. Launching agent decides the name in the call request. should be something like call_agent(class, name, context, task, [extra_tools])
4. Log all agent instaces in the workspace dir: /logs/agentClass_AgentName_startTimestamp.json update log every time the agent finishes or a tool call is done. tool calls will be timestampted in the log file. When a new agent instance is called we povide it with the details about its name, the managing agent name, log location in case it needs to check things it might have forgotten due to context compression. This will be it's persistant long term memory. The logs should be well formated enough to be able to reconstruct a full session if needed later.
5. Simplify the agent call stucture. *call_agent* will continue a session if we use an existing name, otherwise it will start a new session. *dismiss_agent* will clear the session with that name (but leave the log in place). multiple agents of the same class can be launched and reused unless dismissed.
6. when a sub-agent finishes generating we feed back to the managing instance all it generated since the last tool call (sans the tool response) as response. no need to use respond_to_manager tool. The managing agent will then decide what to do with the response. It can continue the session (asking the same name agent again), dismiss the agent, or call another agent. Provide clear feedback to the manager when it ended an istance on where to find the sub-agent log in case it needs to revisit what it generated.